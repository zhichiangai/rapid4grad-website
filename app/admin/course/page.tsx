import Link from "next/link";
import { requireAdminContext } from "@/lib/admin/authorization";
import CourseLessonWorkspace from "@/components/admin/CourseLessonWorkspace";

type Params = Promise<{ module?: string; status?: string; access?: string; q?: string; edit?: string; new?: string; message?: string }>;
type Course = { id: string; slug: string; title: string; is_published: boolean };
type Lesson = { id: string; course_id: string; title: string; slug: string; module_key: string; description: string | null; access_level: "public_preview" | "lab_basic" | "full_course"; video_provider: string; video_external_id: string | null; video_asset_id: string | null; video_status: string; video_upload_id: string | null; material_url: string | null; sort_order: number; is_published: boolean };
const moduleLabels: Record<string, string> = { Research: "Research｜研究", Application: "Application｜應用", Presentation: "Presentation｜表達", Interpersonal: "Interpersonal｜互動", Direction: "Direction｜方向" };
const accessLabels = { public_preview: "公開試看", lab_basic: "Lab 課程", full_course: "完整課程" };
const statusLabels: Record<string, string> = { empty: "尚未上傳", uploading: "正在上傳", processing: "處理中", ready: "可播放", errored: "處理失敗" };
const statusStyles: Record<string, string> = { empty: "border-white/15 bg-white/[0.04] text-slate-300", uploading: "border-amber-300/25 bg-amber-300/10 text-amber-100", processing: "border-amber-300/25 bg-amber-300/10 text-amber-100", ready: "border-emerald-300/25 bg-emerald-300/10 text-emerald-100", errored: "border-red-300/25 bg-red-300/10 text-red-100" };

export default async function AdminCoursePage({ searchParams }: { searchParams: Params }) {
  const params = await searchParams;
  const { admin } = await requireAdminContext("/admin/course");
  const [{ data: courses }, { data: lessonRows }] = await Promise.all([
    admin.from("courses").select("id,slug,title,is_published").order("created_at", { ascending: true }).returns<Course[]>(),
    admin.from("course_lessons").select("id,course_id,title,slug,module_key,description,access_level,video_provider,video_external_id,video_asset_id,video_status,video_upload_id,material_url,sort_order,is_published").order("sort_order", { ascending: true }).returns<Lesson[]>(),
  ]);
  const allCourses = courses ?? [];
  const allLessons = lessonRows ?? [];
  const query = params.q?.trim().toLowerCase() ?? "";
  const lessons = allLessons.filter((lesson) => (!params.module || lesson.module_key === params.module) && (!params.status || (params.status === "published" ? lesson.is_published : !lesson.is_published)) && (!params.access || lesson.access_level === params.access) && (!query || `${lesson.title} ${lesson.slug}`.toLowerCase().includes(query)));
  const explicitLesson = params.edit ? allLessons.find((lesson) => lesson.id === params.edit) ?? null : null;
  const newMode = params.new === "1" || allLessons.length === 0;
  const editing = newMode ? null : explicitLesson ?? lessons[0] ?? null;
  const message = params.message === "saved" ? "課程草稿已儲存。" : params.message === "invalid" ? "請確認必填欄位與影片狀態。" : params.message === "save-failed" ? "儲存失敗，請確認資料。" : params.message === "unpublished" ? "已取消發布，影片與課程資料仍保留。" : params.message === "removed" ? "影片已移除，課程單元仍保留。" : params.message === "removed-association" ? "已移除影片關聯；因沒有可信任的 Mux asset ID，未刪除遠端資產。" : params.message === "deleted" ? "草稿單元已刪除。" : params.message === "delete-has-history" ? "這堂課已有觀看歷史，為保護學習紀錄不能刪除。" : params.message === "delete-processing" ? "影片仍在處理中，請完成或移除影片後再刪除草稿。" : params.message === "delete-blocked" ? "只能刪除草稿，且需要在危險區再次確認。" : params.message === "remove-confirmation" ? "已發布影片需要先確認取消發布並移除。" : params.message === "remove-failed" || params.message === "delete-failed" ? "操作未完成，請稍後再試；遠端影片與課程資料未被不明確地清除。" : null;
  return <section>
    <header className="rounded-[2rem] border border-white/10 bg-slate-950/80 p-6"><p className="text-xs font-semibold uppercase tracking-[0.24em] text-cyan-200">Video Course</p><div className="mt-2 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 className="text-3xl font-semibold">課程影片管理</h1><p className="mt-2 text-sm leading-6 text-slate-400">課程庫、影片狀態、學生顯示位置與上架操作集中在同一個工作區。</p></div><Link href="/admin/course?new=1" className="rounded-xl bg-cyan-400 px-4 py-3 text-center text-sm font-semibold text-slate-950">＋ 新增課程影片</Link></div>{message ? <p className="mt-4 rounded-xl border border-cyan-300/20 bg-cyan-400/10 px-4 py-3 text-sm text-cyan-100" role="status">{message}</p> : null}</header>
    <div className="mt-5 grid gap-5 xl:grid-cols-[1.05fr_0.95fr]"><div><form className="flex flex-col gap-3 rounded-2xl border border-white/10 bg-white/[0.04] p-4 sm:flex-row"><label className="sr-only" htmlFor="course-search">搜尋課程名稱</label><input id="course-search" name="q" defaultValue={params.q} placeholder="搜尋課程名稱" className="min-w-0 flex-1 rounded-xl border border-white/10 bg-slate-950 px-3 py-2 text-sm text-white" /><select name="module" defaultValue={params.module ?? ""} aria-label="依分類篩選" className="rounded-xl border border-white/10 bg-slate-950 px-3 py-2 text-sm text-white"><option value="">全部分類</option>{Object.entries(moduleLabels).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select><select name="status" defaultValue={params.status ?? ""} aria-label="依上架狀態篩選" className="rounded-xl border border-white/10 bg-slate-950 px-3 py-2 text-sm text-white"><option value="">全部上架狀態</option><option value="published">已發布</option><option value="draft">草稿</option></select><button className="rounded-xl bg-blue-500 px-4 py-2 text-sm font-semibold">篩選</button></form><div className="mt-4 overflow-hidden rounded-2xl border border-white/10 bg-white/[0.03]"><div className="hidden grid-cols-[1.5fr_0.85fr_0.85fr_0.8fr] gap-3 border-b border-white/10 px-4 py-3 text-xs text-slate-500 md:grid"><span>課程影片</span><span>觀看對象</span><span>上架狀態</span><span>影片狀態</span></div>{lessons.length ? lessons.map((lesson) => { const selected = editing?.id === lesson.id; const videoLabel = statusLabels[lesson.video_status] ?? lesson.video_status; return <Link key={lesson.id} href={`/admin/course?edit=${lesson.id}`} aria-current={selected ? "page" : undefined} aria-label={`選擇課程影片：${lesson.title}`} className={`grid gap-3 border-b border-white/10 px-4 py-4 transition last:border-0 hover:bg-cyan-400/[0.06] focus-visible:relative focus-visible:z-10 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300 md:grid-cols-[1.5fr_0.85fr_0.85fr_0.8fr] md:items-center ${selected ? "bg-cyan-400/[0.1] shadow-[inset_3px_0_0_#67e8f9]" : ""}`}><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-white">{lesson.title}</p>{selected ? <span className="rounded-full border border-cyan-300/30 px-2 py-0.5 text-[11px] font-semibold text-cyan-100">目前選取</span> : null}</div><p className="mt-1 text-xs text-slate-500">{moduleLabels[lesson.module_key] ?? lesson.module_key}</p></div><p className="text-sm text-slate-300">{accessLabels[lesson.access_level]}</p><p className={`text-sm font-semibold ${lesson.is_published ? "text-emerald-200" : "text-amber-100"}`}>{lesson.is_published ? "已發布" : "未上架"}</p><span className={`inline-flex w-fit rounded-full border px-2.5 py-1 text-xs font-semibold ${statusStyles[lesson.video_status] ?? "border-white/15 text-slate-200"}`}>{videoLabel}</span></Link> }) : <p className="p-8 text-center text-sm text-slate-500">目前沒有符合條件的課程影片。請清除篩選或新增課程影片。</p>}</div></div><CourseLessonWorkspace courses={allCourses} lesson={editing} isNewMode={newMode} defaultSortOrder={(allLessons.at(-1)?.sort_order ?? 0) + 10} /></div>
  </section>;
}
