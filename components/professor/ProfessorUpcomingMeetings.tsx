import Link from "next/link";
import type { AttentionStudent } from "@/lib/professor/attention";

export function ProfessorUpcomingMeetings({ students }: { students: AttentionStudent[] }) {
  const meetings = students.filter((student) => student.nextMeetingAt).sort((a, b) => (a.nextMeetingAt ?? "").localeCompare(b.nextMeetingAt ?? "")).slice(0, 5);
  return <section className="rounded-[2rem] border border-white/10 bg-white/[0.025] p-5 sm:p-6"><p className="text-xs font-semibold uppercase tracking-[0.28em] text-cyan-200">Upcoming Meetings</p><h2 className="mt-2 text-2xl font-semibold text-white">即將到來的 Meeting</h2>{meetings.length > 0 ? <div className="mt-5 divide-y divide-white/10">{meetings.map((student) => <div key={`${student.labId}:${student.studentId}`} className="flex flex-col gap-3 py-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-semibold text-white">{new Intl.DateTimeFormat("zh-TW", { timeZone: "Asia/Taipei", month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit" }).format(new Date(student.nextMeetingAt!))}</p><p className="mt-1 text-sm text-slate-400">{student.name} · {student.labName}</p></div><div className="flex flex-wrap gap-3"><Link href={`/professor/labs/${student.labId}/meetings`} className="text-sm font-semibold text-cyan-100 hover:text-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300/70">開啟 Meeting Center</Link><Link href={`/professor/labs/${student.labId}/students/${student.studentId}`} className="text-sm font-semibold text-slate-300 hover:text-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300/70">查看學生</Link></div></div>)}</div> : <div className="mt-5 rounded-2xl border border-white/10 bg-slate-950/40 p-5"><p className="font-semibold text-slate-200">目前沒有已排程的研究 Meeting</p><p className="mt-2 text-sm text-slate-500">你可以從各 Lab 的 Meeting Center 安排下一次研究討論。</p></div>}</section>;
}
